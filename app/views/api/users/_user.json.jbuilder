json.extract! user, :id, :fname, :lname 
# this is currently assumed to be the user thumnail as seen in a user index/feed/friends list
# might use this in numerous locations
# when the profile photo is present, it will become a part of this partial if possible